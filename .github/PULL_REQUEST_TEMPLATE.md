## Summary

<!-- What does this PR do and why? Reference requirement IDs (e.g. FR-19) if relevant. -->

## Changes

-

## Related requirements / issues

<!-- e.g. Closes #123, implements FR-1..FR-3 -->

## Checklist

- [ ] Conventional Commit title (`feat:`, `fix:`, `docs:`, …)
- [ ] Targets `develop` (or `main` for hotfixes) per Git Flow
- [ ] `uv run ruff check .` and `ruff format --check .` pass
- [ ] `uv run mypy` passes (strict)
- [ ] `uv run pytest` passes; new behaviour has tests
- [ ] No secrets committed; config-driven; errors actionable
- [ ] Docs updated (README / docs / prompts) if behaviour changed
