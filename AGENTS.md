# AGENTS.md

The full guidance for AI coding agents (and humans) working in **SAGA** (core backend)
lives in **[CLAUDE.md](CLAUDE.md)**.

👉 **Read [CLAUDE.md](CLAUDE.md) before making any changes.** It covers the project overview,
tech stack, repository layout, build/test commands, key constraints (config-driven, prompts
in `prompts/*.md`, `saidex` for structured extraction, actionable errors, structured
logging), coding conventions, and the Git commit / branch / pull-request workflow (`main`
and `develop` are protected — use a feature branch + PR).
