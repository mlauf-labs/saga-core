---
tool: add_folder_note
---

Attach a free-text note to a folder (e.g. organisational context). Pass `folder_id` and
`content`. Notes carry creation and modification timestamps. Returns the created note.
