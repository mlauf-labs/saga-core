---
tool: set_document_folders
---

Replace a document's entire folder membership set in one call. Pass `document_id` and
`folder_ids` (the complete desired set); optionally `primary_id` to mark the canonical
folder. Returns the updated folder set.
