---
tool: update_doc_type
---

Update a document type's `name`, `description`, and/or `emoji`. Pass `doc_type_id` and
the fields to change. The `emoji` is a single emoji for visual display and is NOT part
of the name. Renames are re-projected to all documents of that type. Returns the updated
doc-type.
