---
tool: set_primary_folder
---

Mark one of a document's existing folders as its primary (canonical) folder, which
drives the backup layout. Pass `document_id` and `folder_id` (must already be a member).
Returns the updated folder set.
